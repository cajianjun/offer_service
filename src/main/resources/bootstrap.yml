
spring:
  cloud:
    config:
      name: searchapi
      discovery:
        service-id: config
        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/
